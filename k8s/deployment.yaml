apiVersion: apps/v1
kind: Deployment
metadata:
  name: ggcl-math-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ggcl-math-service
  template:
    metadata:
      labels:
        app: ggcl-math-service
    spec:
      containers:
      - name: ggcl-math-service
        image: hoanghiep4298/math-service:latest
        ports:
        - containerPort: 8800                 # ggcl-math-service chạy port 8800
---
apiVersion: v1
kind: Service
metadata:
  name: ggcl-math-service
spec:
  selector:
    app: ggcl-math-service
  type: ClusterIP               # chỉ nội bộ cluster, không public ra ngoài
  ports:
    - port: 8800
      targetPort: 8800
